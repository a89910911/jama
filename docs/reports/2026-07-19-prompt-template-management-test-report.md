# 提示词模板数据库化测试报告

> 测试日期：2026-07-19  
> 测试结论：通过  
> 对应需求：[提示词模板数据库化需求文档](../plans/2026-07-19-prompt-template-management-requirements.md)  
> 对应计划：[提示词模板数据库化实施计划](../plans/2026-07-19-prompt-template-management-implementation-plan.md)

## 1. 测试环境

| 项目 | 值 |
|---|---|
| 操作系统 | Windows |
| Node.js | v24.15.0 |
| npm | 11.12.1 |
| 后端 | Express + better-sqlite3 |
| 前端 | Vue 3 + Vite 5.4.21 |
| 测试数据库 | SQLite `:memory:`，另对本地页面执行只读/草稿交互 |

## 2. 测试范围

- 新数据库迁移和系统模板初始化。
- 102 个定义、146 条多语言模板的完整性与幂等性。
- 系统级编辑、草稿预览、版本冲突和恢复出厂默认。
- 项目级覆盖优先、项目隔离、系统回退和删除覆盖。
- `zh` / `en` / `universal` 解析策略。
- 模板变量白名单、必填变量、括号和渲染校验。
- 软件升级时默认种子更新与用户自定义保护。
- 旧 `prompt_overrides` 和旧业务场景映射兼容迁移。
- 异步任务提示词快照。
- 图片、视频、负向提示词和技术拼装模板。
- 20 个业务场景注册和 AI 调用方静态审计。
- 系统/项目 API 真实 HTTP 流程。
- 前端完整列表、筛选、来源展示、风险提示、草稿预览和未保存提醒。
- 前端生产构建。

## 3. 自动化测试结果

### 3.1 后端

命令：

```bash
cd backend-node
node --test test/*.test.js
```

结果：

| 指标 | 数量 |
|---|---:|
| 测试套件 | 11 |
| 测试用例 | 44 |
| 通过 | 44 |
| 失败 | 0 |
| 跳过 | 0 |

提示词专项用例 11/11 通过，覆盖：

- 全量目录初始化和重复初始化。
- 未编辑种子自动升级、已编辑系统内容保护。
- 项目覆盖优先、项目隔离和项目上下文推导。
- 同语言和 `universal` 回退，不跨中英文。
- 变量合法性、版本冲突和恢复出厂默认。
- 旧覆盖和旧场景路由迁移。
- 异步任务快照不可变。
- 图片/视频技术拼装和项目覆盖。
- 系统/项目 API 编辑、草稿预览、删除覆盖。
- 20 个业务场景注册。
- 全部提示词 key 存在运行时调用方、全部 AI 路由使用已注册场景。

### 3.2 前端

命令：

```bash
cd frontweb
node --test test/*.test.js
```

结果：

| 指标 | 数量 |
|---|---:|
| 测试用例 | 5 |
| 通过 | 5 |
| 失败 | 0 |

覆盖默认/指定模型配置、提示词行稳定标识、系统/项目有效内容选择和组合筛选。

### 3.3 生产构建

命令：

```bash
cd frontweb
npm run build
```

结果：构建通过，1627 个模块完成转换。

Vite 报告主包压缩前大于 500 kB。这是性能优化告警，不影响本需求功能和部署产物生成。

## 4. 真实 HTTP 全流程

使用当前 Express 应用和独立 SQLite 内存数据库，通过随机空闲端口执行真实 HTTP 请求。

| 验证项 | 结果 |
|---|---|
| 系统列表 | 146 条 |
| 唯一提示词定义 | 102 个 |
| 单 key + locale 查询 | 通过 |
| 系统保存后版本递增 | 通过 |
| 当前编辑草稿预览 | 通过 |
| 草稿预览不提前写库 | 通过 |
| 系统恢复出厂默认 | 通过 |
| 项目列表 | 146 条 |
| 无覆盖时项目继承 | 146/146 来自系统 |
| 创建项目覆盖 | 通过 |
| 项目覆盖渲染 | 通过 |
| 删除覆盖恢复系统 | 通过 |
| 业务场景注册表 | 20 个 |
| `role_extraction` | 已注册 |

## 5. 浏览器页面全流程

使用当前生产构建和临时 5680 后端验证，完成后已停止临时服务；没有保存测试草稿。

| 页面场景 | 结果 |
|---|---|
| 打开“高级设置（提示词）” | 通过 |
| 列表汇总 | 显示“共 146 条（102 个提示词定义）” |
| 页面实际列表节点 | 146 |
| 编辑低风险模板 | 通过，显示脏数据标记并启用保存按钮 |
| 预览未保存草稿 | 渲染为 `BROWSER DRAFT [character_appearance]` |
| 草稿预览错误提示 | 无 |
| 切换提示词未保存提醒 | 正确显示“未保存修改” |
| 选择“继续编辑” | 当前 key 和草稿内容保持 |
| 选择“放弃修改” | 切换成功，脏数据标记清除 |
| 业务场景下拉 | 20 个选项 |
| `role_extraction` 场景 | 可见 |
| 页面错误提示 | 无 |

## 6. 静态和质量检查

- `git diff --check` 通过，没有空白错误。
- 目录变量契约校验通过：102 个定义、146 条模板无未注册变量或缺失必填变量。
- 静态测试确认每个目录 key 都存在运行时调用方。
- 静态测试确认每个 AI 调用使用的 `scene_key` 都在注册表中。
- 删除未使用的旧提示词覆盖路由/服务和未使用的硬编码分镜上下文函数。
- 本次没有修改或回退用户实施前已有的 `backend-node/package-lock.json` 改动。

## 7. 验收结论

本需求范围内的数据库、运行时解析、系统/项目两级管理、多语言、异步快照、恢复出厂、业务场景路由、前端页面和兼容迁移均已通过测试，可以使用。

## 8. 已知限制

- 测试环境没有配置真实外部 AI 供应商凭据，因此没有实际消耗额度调用 Claude、DeepSeek、图片或视频供应商。调用前的模型路由、提示词解析、拼装、快照和请求入口均已通过自动化与静态验证。
- 前端主包存在 Vite 大于 500 kB 的性能告警，和本需求正确性无关。
